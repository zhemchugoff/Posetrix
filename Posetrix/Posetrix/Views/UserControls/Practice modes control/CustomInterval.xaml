<UserControl
    x:Class="Posetrix.Views.UserControls.CustomInterval"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Posetrix.Views.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    DataContext="{Binding Path=CustomIntervalViewModel, Source={StaticResource Locator}}"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBox
            Grid.Column="0"
            Height="30"
            Margin="0,0,10,0"
            Text="{Binding SessionTimer.Hours}" />
        <TextBox
            Grid.Column="1"
            Height="30"
            Margin="0,0,10,0"
            Text="{Binding SessionTimer.Minutes}" />
        <TextBox
            Grid.Column="2"
            Height="30"
            Text="{Binding SessionTimer.Seconds}" />
    </Grid>
</UserControl>
